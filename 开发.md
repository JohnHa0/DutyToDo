# 值班通知辅助助手 - 开发与运行文档

本文档详细说明了“值班通知辅助助手”的开发配置、依赖要求以及如何打包发布。

## 1. 环境依赖与架构说明

本项目采用了 **Electron + React + FastAPI** 混合架构，旨在提供出色的系统原生通知体验，同时保证轻量的本地化部署。

### 为什么选择 `venv` 而不是 `Conda`？
针对您的疑问，这里做个说明：在开发用于终端桌面发布的单机软件时，**我们极力推荐使用标准的 Python `venv` 虚拟环境，而非 Conda**。
*   **体积优势**：Conda 包含大量基础数据科学包和复杂的环境管理工具，使用 Conda 打包（如借助 PyInstaller 或 Nuitka）往往会导致最终的安装包体积膨胀到 1GB 以上。而纯净的 `venv` 只包含我们通过 `pip` 手动安装的 FastAPI 等核心依赖，打包后的体积可以控制在 100MB 左右。
*   **系统兼容性**：在 Ubuntu 和老旧的 Kylin 系统上，原生的 Python `venv` 具有极佳的开箱即用特性，几乎不依赖外部的环境变量注入。

## 2. 开发环境配置 (Development Setup)

### 2.1 后端配置 (FastAPI)
1.  **进入项目目录**：
    ```bash
    cd backend
    ```
2.  **创建虚拟环境**（如果尚未创建）：
    ```bash
    python3 -m venv duty
    ```
3.  **激活虚拟环境**：
    *   Ubuntu / Kylin / macOS: `source venv/bin/activate`
    *   Windows: `venv\Scripts\activate`
4.  **安装依赖**：
    ```bash
    pip install fastapi uvicorn sqlalchemy jionlp pydantic python-multipart
    ```
5.  **启动后端服务**：
    ```bash
    uvicorn main:app --reload --port 8000
    ```
    *   API 文档会自动生成在：`http://localhost:8000/docs`

### 2.2 前端配置 (React + Electron)
1.  **进入项目目录**：
    ```bash
    cd frontend
    ```
2.  **安装 Node 依赖**：
    ```bash
    npm install
    ```
3.  **启动开发环境 (双重启动)**：
    ```bash
    npm run dev
    ```
    *   *说明：由于我们配置了 `concurrently`，此命令会自动在后台启动 Vite 开发服务器 (React)，并随后唤起 Electron 桌面窗口加载您的应用，实现代码的热更新 (HMR)。*

## 3. 打包与发布指南 (Packaging for Release)

### 3.1 前后端集成打包思路
最终发布时，我们需要将后端和前端打包为一个 `.deb` 或 `.AppImage` 文件（Linux 常用单文件分发格式）。

1.  **打包 Python 后端**：
    使用 `PyInstaller` 将 `main.py` 连同其依赖打包为一个独立的可执行二进制文件。
    ```bash
    pip install pyinstaller
    pyinstaller -F main.py
    ```
2.  **构建前端与 Electron**：
    ```bash
    cd frontend
    npm run electron:build
    ```
    *在 `electron-builder` 的配置中，我们将编译好的 Python 二进制文件作为“额外资源 (extraResources)”一并打包进最终的安装程序中，并在 Electron 启动时由 Node.js 自动生成子进程唤起 Python 后端。*

## 4. 目录结构

```text
DutyToDo/
├── backend/            # Python FastAPI 后端
│   ├── main.py         # 接口入口及 NLP 处理逻辑
│   ├── models.py       # 数据库模型设计
│   ├── schemas.py      # 数据校验与 API 传输实体
│   ├── database.py     # SQLite 数据库配置
│   └── data/           # SQLite 数据文件与附件存储目录
│
├── frontend/           # React + Electron 前端
│   ├── src/            # React UI 源码
│   ├── main.cjs        # Electron 主进程入口 (唤起窗口、系统通知)
│   ├── preload.cjs     # 预加载脚本 (安全桥接系统 API)
│   ├── package.json    # 前端及打包配置文件
│   └── vite.config.ts  # 编译配置
```
