# 值班助手 (DutyToDo) - 麒麟 OS (DEB) 发布打包指南

由于我们的系统由 **FastAPI 后端** 和 **Electron + React 前端** 两部分组成，在目标国产操作系统（如银河麒麟 Kylin OS）上发布独立的 `.deb` 安装包，最佳实践是将后端打包为独立可执行文件，并将其内置到 Electron 前端包中。

以下是完整的发布流程（建议直接在目标麒麟系统的同架构机器上执行）：

## 步骤一：打包后端 (Python FastAPI) 为独立二进制程序

为了让麒麟系统的用户无需手动安装 Python、pip 和一大堆环境依赖，我们需要使用 `PyInstaller` 将后端打包成一个不需要任何依赖的绿色二进制文件。

1. **进入后端目录并安装打包工具**：
   ```bash
   cd backend
   pip install pyinstaller
   ```
2. **执行 PyInstaller 打包**：
   ```bash
   pyinstaller --name backend_server --onefile --noconsole main.py
   ```
3. **验证结果**：
   打包完成后，会在 `backend/dist/` 目录下生成一个名为 `backend_server` 的可执行文件。此时您可以尝试直接运行它 `./dist/backend_server`，测试接口是否能在不依赖 python 环境的情况下正常启动。

---

## 步骤二：配置 Electron 集成后端

我们需要让 Electron 在启动时，自动在后台拉起刚刚打包好的 `backend_server`。

1. **修改前端 `electron/main.ts` (或 `main.js`)**：
   在 Electron 创建主窗口的代码中，加入启动后端的逻辑（大致代码如下）：
   ```javascript
   const { app, BrowserWindow } = require('electron');
   const { spawn } = require('child_process');
   const path = require('path');

   let backendProcess;

   function createWindow() {
     // 根据是否打包，确定后端二进制文件的路径
     const backendPath = app.isPackaged 
       ? path.join(process.resourcesPath, 'backend_server')
       : path.join(__dirname, '../../backend/dist/backend_server');
       
     // 启动后端服务
     backendProcess = spawn(backendPath, [], { detached: false });
     
     // 正常的 Electron 窗口创建代码...
     const win = new BrowserWindow({ width: 1200, height: 800 });
     win.loadURL('http://127.0.0.1:8000'); // 或者加载本地 file://
   }

   // 确保退出时杀掉 Python 后端进程
   app.on('will-quit', () => {
     if (backendProcess) {
       backendProcess.kill();
     }
   });
   ```

## 步骤三：配置前端打包参数 (DEB)

在 `frontend/package.json` 中配置 `electron-builder`，指定目标为 Linux 的 `deb` 格式，并将后端的二进制文件包含进安装包内。

1. **修改 `frontend/package.json`**：
   找到或添加 `build` 节点：
   ```json
   "build": {
     "appId": "com.dutytodo.app",
     "productName": "值班助手",
     "linux": {
       "target": ["deb"],
       "category": "Office",
       "icon": "build/icons"
     },
     "extraResources": [
       {
         "from": "../backend/dist/backend_server",
         "to": "backend_server"
       }
     ]
   },
   "scripts": {
     "build": "vite build",
     "package:linux": "electron-builder --linux deb"
   }
   ```

## 步骤四：执行最终打包 (生成 DEB)

在确保前端代码已经全部编写并测试完毕后，在麒麟终端中执行：

1. **编译前端静态资源**：
   ```bash
   cd frontend
   npm run build
   ```
2. **构建 DEB 安装包**：
   ```bash
   npm run package:linux
   ```
3. **获取安装包**：
   打包完成后，在 `frontend/dist/` (或 `frontend/build/`) 目录下，您会找到类似 `值班助手_1.1.0_amd64.deb` 或 `_arm64.deb` 的安装包。

## 架构提示 (CPU 架构)
由于麒麟操作系统硬件繁杂：
- 如果您的机器是 **飞腾/鲲鹏** 芯片，打包出的 `.deb` 是 `arm64` 架构。
- 如果您的机器是 **兆芯/海光** 芯片，打包出的是 `amd64 (x86_64)` 架构。
> **强烈建议：** 直接在目标芯片架构的麒麟系统上执行上述完整打包流程，以保证底层依赖的一致性。
